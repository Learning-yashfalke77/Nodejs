// Name of filee  should be index because it acts as an entry point in any directory
// This 

const blue = require('./blue')
const sadie = require('./sadie')
const janet = require('./janet')

const allCats  = [blue, sadie, janet]
// console.log(allCats)

// To Import module of an entire directory 
module.exports = allCats