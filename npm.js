// Node package manager
// A command line tool to easily install and mange packages in  node.js


// If you do: [npm install] it will look at package.json and install its all dependencies

// ----------------------------------------------- INSTALLING LOCALLY ---------------------------------------------------------------------
// to install jokes: npm install give-me-a-joke
//            colors: npm install colors

// -------------------------------------------------- Install globally ------------------------------------------------------------------
// npm i -g cowsay
// To link global package to your dir : npm link cowsay


// ----------------------------------------------- Package.json -------------------------------------------------------------

// Every package has package.json which contains metadata of package
// It also has dependencies
// Dependencies contains all the package installed on that project, for that project
// Every node should have its package.json